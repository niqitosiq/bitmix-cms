import { ComponentList } from '@widgets/ComponentList'
import { SiteList } from '@widgets/SiteList'

export const Page = () => {
    return (
        <div>
            <SiteList />

            <ComponentList />
        </div>
    )
}
